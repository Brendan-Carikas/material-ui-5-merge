import React from 'react';
import { Typography } from '@mui/material';
import FixedBody from '../FixedBody';

export default (
  <FixedBody>
    <Typography
      variant="h4"
    >
      Test title
    </Typography>
  </FixedBody>
)
